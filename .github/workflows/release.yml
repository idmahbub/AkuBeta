name: Auto Release Build

on:
  push:
    branches: [main, master]
  workflow_dispatch:

permissions:
  contents: write

jobs:

  # =========================
  # VERSION TAGGING
  # =========================
  version:
    runs-on: ubuntu-latest
    outputs:
      new_tag: ${{ steps.create_tag.outputs.new_tag }}
    steps:
      - uses: actions/checkout@v4
      - name: Fetch tags
        run: git fetch --tags
      - name: Determine new tag
        id: create_tag
        run: |
          latest=$(git tag --sort=-v:refname | head -n1)
          if [ -z "$latest" ]; then
            new_tag="v1.0.0"
          else
            version=${latest#v}
            IFS='.' read -r major minor patch <<< "$version"
            patch=$((patch+1))
            new_tag="v$major.$minor.$patch"
          fi
          echo "new_tag=$new_tag" >> $GITHUB_OUTPUT
          git tag $new_tag
          git push origin $new_tag

  # =========================
  # BUILD CROSS PLATFORM
  # =========================
  build:
    needs: version
    strategy:
      matrix:
        os: [windows-latest, macos-latest, ubuntu-latest]
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v4
      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"
      - name: Install dependencies
        run: pip install pyinstaller yt-dlp

      - name: Build Windows
        if: runner.os == 'Windows'
        run: pyinstaller app.py --onefile --windowed --name PlaylistApp-windows --add-data "fonts;fonts" --add-data "bin;bin"
        shell: cmd

      - name: Build Mac
        if: runner.os == 'macOS'
        run: pyinstaller app.py --onefile --windowed --name PlaylistApp-macos --add-data "fonts:fonts" --add-data "bin:bin"

      - name: Build Linux
        if: runner.os == 'Linux'
        run: pyinstaller app.py --onefile --windowed --name PlaylistApp-linux --add-data "fonts:fonts" --add-data "bin:bin"

      - name: Upload artifact per OS
        uses: actions/upload-artifact@v4
        with:
          name: build-${{ matrix.os }}
          path: dist/*

  # =========================
  # CREATE RELEASE
  # =========================
  release:
    needs: build
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0   # penting agar tag tersedia

      - name: Download Windows artifact
        uses: actions/download-artifact@v4
        with:
          name: build-windows-latest
          path: artifacts/windows

      - name: Download Mac artifact
        uses: actions/download-artifact@v4
        with:
          name: build-macos-latest
          path: artifacts/macos

      - name: Download Linux artifact
        uses: actions/download-artifact@v4
        with:
          name: build-ubuntu-latest
          path: artifacts/linux

      - name: Create GitHub Release
        uses: ncipollo/release-action@v1
        with:
          tag: ${{ needs.version.outputs.new_tag }}
          name: Release ${{ needs.version.outputs.new_tag }}
          artifacts: |
            artifacts/windows/PlaylistApp-windows.exe
            artifacts/macos/PlaylistApp-macos
            artifacts/linux/PlaylistApp-linux
          draft: false
          prerelease: false
          token: ${{ secrets.GITHUB_TOKEN }}
